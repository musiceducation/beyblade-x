# SSL certificates (generate locally with serve-https.py)
cert.pem
key.pem

# macOS
.DS_Store

# DJI RTMP relay temp frame
.dji-frame.jpg
.dji-ffmpeg.log

# Server-side replay uploads
.replays/

# Supabase host credentials (copy from arena-config.example.js)
arena-config.local.js
arena-secrets.local.json
